<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:paddingStart">8dp</item> <!-- Sesuaikan padding start yang diinginkan -->
        <item name="android:paddingEnd">8dp</item> <!-- Sesuaikan padding end yang diinginkan -->
        <item name="android:buttonStyle">@style/AppTheme.BackButton</item>
    </style>

    <style name="AppTheme.BackButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:padding">8dp</item> <!-- Sesuaikan ukuran padding yang diinginkan -->
    </style>

</resources>